---
import Link from '@/components/Link.astro'
import Text from '@/components/Text.astro'
import MapGrid from '@/components/MapGrid.astro'
import Subtitle from '@/components/Subtitle.astro'
import { DISCORD_LINK } from '@/shared/constants.js'
---

<section id="contributors" class="py-16 lg:pt-28 lg:pb-12">
  <div class="container grid gap-6 px-3 mx-auto max-w-4xl justify-items-center lg:px-0 lg:gap-20 lg:gap-x-16 lg:gap-y-4 2xl:max-w-6xl">
    <Subtitle as="h2">Ellos son parte de nuestra comunidad</Subtitle>
    <article class="map-community lg:[grid-area:mapgrid] lg:self-center">
      <MapGrid />
    </article>
    <article class="message grid gap-6 justify-items-center lg:[grid-area:message] lg:justify-items-start">
      <Text>
        En los proyectos de la comunidad hay personas del todo el mundo que
        aporta su tiempo, conocimiento y experiencia con los demás.
      </Text>
      <Link external to={DISCORD_LINK} className="btn primary fill large">Conócelos a todos</Link>
    </article>
  </div>
</section>

<style lang="postcss">
  @screen lg {
    .container {
      grid-template-areas:
        'mapgrid subtitle'
        'mapgrid message';
    }
  }
</style>
