---
import Navbar from '@/components/Navbar.astro'
import Footer from '@/components/Footer.astro'

const {
  withoutNavbar = false,
  withoutFooter = false,
  title = 'Fazt Community',
  description = 'Crece como profesional compartiendo tus conocimientos con los demás y participando en proyectos open source',
} = Astro.props
---

<!doctype html>
<html lang="es" class="[scroll-behavior:smooth]">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="#EC0B2B" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <link rel="apple-touch-icon" href="favicon.png" />
    <link rel="icon" type="image/png" sizes="64x64" href="favicon.png" />

    <meta name="generator" content={Astro.generator} />
  </head>

  <body class="min-h-screen dark:bg-gray-400 dark:text-white">
    {!withoutNavbar && <Navbar />}

    <slot />

    {!withoutFooter && <Footer />}
  </body>
</html>
